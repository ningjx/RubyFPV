# UI优化实施计划 - 第一、二阶段

## 1. 实施概述

### 1.1 第一阶段：Cairo Context复用（Radxa专用） ✅ 已完成

**目标**: 避免每帧创建/销毁Cairo context，减少1-1.5ms开销

**改动范围**: 仅修改 `render_engine_cairo.cpp`

**风险**: 中等（需处理双缓冲切换）

**状态**: ✅ 已实施完成

### 1.2 第二阶段：共享逻辑改动 ✅ 已完成

**目标**: 
- 数值变化检测：减少不必要的重绘
- 统一渲染调度：稳定帧率，避免重复渲染

**改动范围**: 
- `osd.cpp` - 数值变化检测
- `ruby_central.cpp` - 统一渲染调度
- `events.cpp`, `warnings.cpp`, `menu_*.cpp` 等 - 替换render_all调用

**风险**: 低（平台无关）

**状态**: ✅ 已实施完成（数值变化检测暂未实施，仅实施了统一渲染调度）

---

## 2. 第一阶段详细改动（已完成）

### 2.1 render_engine_cairo.h 改动 ✅

**位置**: `code/renderer/render_engine_cairo.h`

**添加成员变量**:
```cpp
// Context复用优化相关变量
u32 m_uLastBufferId;           // 上次使用的缓冲区ID
bool m_bContextReuseEnabled;   // Context复用开关
```

### 2.2 render_engine_cairo.cpp 改动 ✅

**位置**: `code/renderer/render_engine_cairo.cpp`

**改动1: 初始化新变量** (构造函数中)
```cpp
// Context复用优化初始化
m_uLastBufferId = 0xFFFFFFFF;  // 初始化为无效值
m_bContextReuseEnabled = true; // 默认启用复用
```

**改动2: startFrame()修改**
- 添加缓冲区切换检测逻辑
- 缓冲区未切换时复用现有context
- 缓冲区切换时重建context
- 复用时重置Cairo状态（变换矩阵、裁剪区域）

**改动3: endFrame()修改**
- Context复用启用时不销毁context
- Context复用禁用时保持原有逻辑（销毁context）

---

## 3. 第二阶段详细改动（已完成）

### 3.1 ruby_central.h 改动 ✅

**位置**: `code/r_central/ruby_central.h`

**添加导出函数声明**:
```cpp
// 渲染调度函数（第二阶段优化）
void request_render();              // 请求渲染（可能延迟）
void request_render_immediate();    // 强制立即渲染
```

### 3.2 ruby_central.cpp 改动 ✅

**位置**: `code/r_central/ruby_central.cpp`

**改动1: 添加渲染调度器变量**
```cpp
// 渲染调度器变量（第二阶段优化）
static u32 s_lastRenderSchedulerTime = 0;
static u32 s_minRenderInterval = 16;  // 约60fps (16ms)
static bool s_renderRequested = false;
static bool s_forceImmediateRender = false;
```

**改动2: 实现渲染调度函数**
- `request_render()` - 请求渲染（可能延迟）
- `request_render_immediate()` - 强制立即渲染
- `should_render()` - 判断是否应该渲染
- `execute_scheduled_render()` - 执行调度渲染

**改动3: 修改主循环**
- 使用渲染调度器替代直接render_all调用
- 自动请求渲染（基于FPS设置）

### 3.3 events.cpp 改动 ✅

**位置**: `code/r_central/events.cpp` (line 194)

**改动**: 替换 `render_all(g_TimeNow)` 为 `request_render_immediate()`

### 3.4 warnings.cpp 改动 ✅

**位置**: `code/r_central/warnings.cpp` (line 330)

**改动**: 替换 `render_all(g_TimeNow)` 为 `request_render_immediate()`

### 3.5 menu_controller_update.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_controller_update.cpp`

**改动**: 替换多处 `render_all(g_TimeNow)` 为 `request_render_immediate()`

### 3.6 menu_search.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_search.cpp`

**改动**: 替换多处 `render_all(get_current_timestamp_ms(), true)` 为 `request_render_immediate()`

### 3.7 forward_watch.cpp 改动 ✅

**位置**: `code/r_central/forward_watch.cpp`

**改动**: 替换多处 `render_all(g_TimeNow)` 为 `request_render_immediate()`

### 3.8 handle_commands.cpp 改动 ✅

**位置**: `code/r_central/handle_commands.cpp` (line 1646-1648)

**改动**: 替换配对过程中的 `render_all(g_TimeNow)` 为 `request_render_immediate()`

### 3.9 menu_controller_network.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_controller_network.cpp` (line 204)

**改动**: 替换SSH启用循环中的 `render_all(g_TimeNow)` 为 `request_render_immediate()`

### 3.10 menu_storage.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_storage.cpp`

**改动**: 替换多处文件复制/移动进度显示中的 `render_all()` 为 `request_render_immediate()`
- 截图复制进度
- 视频复制进度
- USB挂载进度
- 视频处理等待循环

### 3.11 menu_switch_vehicle.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_switch_vehicle.cpp` (line 135-138)

**改动**: 替换车辆切换过程中的 `render_all()` 为 `request_render_immediate()`

### 3.12 menu_system_dev_logs.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_system_dev_logs.cpp` (line 221)

**改动**: 替换日志导出过程中的 `render_all(g_TimeNow)` 为 `request_render_immediate()`

### 3.13 menu_vehicle_camera.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_vehicle_camera.cpp` (line 1353)

**改动**: 替换相机更新进度中的 `render_all(g_TimeNow)` 为 `request_render_immediate()`

### 3.14 menu_vehicles.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_vehicles.cpp` (line 215)

**改动**: 替换车辆删除过程中的 `render_all()` 为 `request_render_immediate()`

### 3.15 menu_vehicle_selector.cpp 改动 ✅

**位置**: `code/r_central/menu/menu_vehicle_selector.cpp`

**改动**: 替换多处车辆选择/删除过程中的 `render_all()` 为 `request_render_immediate()`

---

## 4. 编译验证 ✅

所有改动文件编译验证通过，无错误：

| 文件 | 编译状态 |
|------|----------|
| `render_engine_cairo.cpp` | ✅ 无错误 |
| `render_engine_cairo.h` | ✅ 无错误 |
| `ruby_central.cpp` | ✅ 无错误 |
| `ruby_central.h` | ✅ 无错误 |
| `events.cpp` | ✅ 无错误 |
| `warnings.cpp` | ✅ 无错误 |
| `menu_controller_update.cpp` | ✅ 无错误 |
| `menu_search.cpp` | ✅ 无错误 |
| `forward_watch.cpp` | ✅ 无错误 |
| `handle_commands.cpp` | ✅ 无错误 |
| `menu_controller_network.cpp` | ✅ 无错误 |
| `menu_storage.cpp` | ✅ 无错误 |
| `menu_switch_vehicle.cpp` | ✅ 无错误 |
| `menu_system_dev_logs.cpp` | ✅ 无错误 |
| `menu_vehicle_camera.cpp` | ✅ 无错误 |
| `menu_vehicles.cpp` | ✅ 无错误 |
| `menu_vehicle_selector.cpp` | ✅ 无错误 |

---

## 5. 未替换的render_all调用（保持原有逻辑）

以下调用保持原有逻辑，不进行替换：

| 文件 | 原因 |
|------|------|
| `ruby_central.cpp` (line 274) | 调度器内部核心渲染调用 |
| `ruby_central.cpp` (line 2473) | 启动序列渲染 |
| `video_playback.cpp` (line 199, 210) | 独立视频播放渲染路径 |
| `osd.cpp` (line 2882) | OSD核心渲染函数定义 |

---

## 6. 待实施改动

### 5.1 数值变化检测（第二阶段剩余）

**位置**: `code/r_central/osd/osd.cpp`

**待添加**:
- 数值变化检测变量
- 电压、速度、高度等显示函数的变化检测逻辑

### 5.2 其他render_all调用替换

**待替换文件**:
- `menu_controller_network.cpp`
- `video_playback.cpp` (保持原有调用，独立渲染路径)
- 其他menu文件中的render_all调用

---

## 6. 测试验证清单

### 6.1 第一阶段验证

| 验证项 | 验证方法 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 编译通过 | `make all RUBY_BUILD_ENV=radxa` | 无编译错误 | ✅ 已验证 |
| Context复用生效 | 添加日志输出 | 日志显示Context复用次数 | 待验证 |
| 双缓冲切换正确 | 触发缓冲区切换 | 无渲染错误 | 待验证 |
| 功能正常 | 运行程序测试 | UI显示正确 | 待验证 |

### 6.2 第二阶段验证

| 验证项 | 验证方法 | 预期结果 | 状态 |
|--------|----------|----------|------|
| 编译通过 | `make all RUBY_BUILD_ENV=radxa` | 无编译错误 | ✅ 已验证 |
| 编译通过 | `make all RUBY_BUILD_ENV=pi` | 无编译错误 | 待验证 |
| 渲染调度生效 | 添加日志输出 | 帧率稳定在60fps | 待验证 |
| 告警立即显示 | 触发告警 | 告警立即出现 | 待验证 |
| 功能正常 | 运行程序测试 | UI显示正确 | 待验证 |

---

## 7. 回滚方案

### 7.1 第一阶段回滚

```cpp
// 在 render_engine_cairo.cpp 中设置开关
m_bContextReuseEnabled = false;  // 禁用复用，恢复原有逻辑
```

### 7.2 第二阶段回滚

```cpp
// 在 ruby_central.cpp 中修改
// 将 request_render() 和 request_render_immediate() 替换回 render_all()
```

---

*文档更新日期: 2026年4月26日*
*RubyFPV 版本: 11.8 (Build 11801)*
*实施状态: 第一阶段完成，第二阶段部分完成*
   
   if ( m_bContextReuseEnabled && !bBufferSwitched && NULL != m_pCairoCtx )
   {
      // 缓冲区未切换，复用现有context
      // 重置Cairo状态
      cairo_identity_matrix(m_pCairoCtx);
      cairo_reset_clip(m_pCairoCtx);
   }
   else
   {
      // 缓冲区切换或首次创建，需要重建context
      if ( NULL != m_pCairoCtx )
         cairo_destroy(m_pCairoCtx);
      m_pCairoCtx = NULL; 

      if ( pOutputBufferInfo->uBufferId == m_uRenderDrawSurfacesIds[0] )
      if ( NULL != m_pMainCairoSurface[0] )
         m_pCairoCtx = cairo_create(m_pMainCairoSurface[0]);

      if ( pOutputBufferInfo->uBufferId == m_uRenderDrawSurfacesIds[1] )
      if ( NULL != m_pMainCairoSurface[1] )
         m_pCairoCtx = cairo_create(m_pMainCairoSurface[1]);
      
      m_uLastBufferId = pOutputBufferInfo->uBufferId;
   }

   // === 保持原有全屏清除逻辑（暂不改动）===
   memset(pOutputBufferInfo->pData, m_uClearBufferByte, pOutputBufferInfo->uSize);

   if ( NULL == m_pCairoCtx )
      return;
}
```

**改动3: endFrame()修改**
```cpp
void RenderEngineCairo::endFrame()
{
   if ( ! m_bStartedFrame )
   {
      log_softerror_and_alarm("[RenderEngineCairo] Tried to double end a render frame.");
      return;
   }

   // === 新增: Context复用时不销毁 ===
   if ( !m_bContextReuseEnabled )
   {
      // 复用禁用时，销毁context（保持原有逻辑）
      if ( NULL != m_pCairoCtx )
         cairo_destroy(m_pCairoCtx);
      m_pCairoCtx = NULL; 
   }

   // 保持原有逻辑：销毁临时context
   if ( NULL != m_pCairoTempCtx )
      cairo_destroy(m_pCairoTempCtx);
   m_pCairoTempCtx = NULL;

   ruby_drm_swap_mainback_buffers();
   RenderEngine::endFrame();
}
```

---

## 3. 第二阶段详细步骤

### 3.1 步骤清单

| 步骤 | 文件 | 改动内容 | 验证方法 |
|------|------|----------|----------|
| 1 | `ruby_central.cpp` | 添加渲染调度器变量和函数 | 编译通过 |
| 2 | `ruby_central.cpp` | 修改主循环使用调度器 | 编译通过 |
| 3 | `ruby_central.h` | 导出渲染调度函数 | 编译通过 |
| 4 | `events.cpp` | 替换render_all为request_render | 编译通过 |
| 5 | `warnings.cpp` | 替换render_all为request_render_immediate | 编译通过 |
| 6 | `menu_controller_update.cpp` | 替换render_all为request_render_immediate | 编译通过 |
| 7 | `menu_search.cpp` | 替换render_all为request_render_immediate | 编译通过 |
| 8 | `forward_watch.cpp` | 替换render_all为request_render | 编译通过 |
| 9 | `video_playback.cpp` | 保持原有调用（独立渲染路径） | 编译通过 |
| 10 | `osd.cpp` | 添加数值变化检测变量 | 编译通过 |
| 11 | `osd.cpp` | 修改电压显示函数 | 编译通过 |
| 12 | `osd.cpp` | 修改速度显示函数 | 编译通过 |
| 13 | `osd.cpp` | 修改高度显示函数 | 编译通过 |
| 14 | 所有平台 | 功能测试 | 显示正确 |

### 3.2 详细改动内容

#### 步骤1-3: ruby_central.cpp/h 改动

**位置**: `code/r_central/ruby_central.cpp`

**改动1: 添加渲染调度器** (文件顶部，全局变量区域)
```cpp
// === 新增: 渲染调度器 ===
static u32 s_lastRenderTime = 0;
static u32 s_minRenderInterval = 16;  // 约60fps (16ms)
static bool s_renderRequested = false;
static bool s_forceImmediateRender = false;

// 新增: 渲染调度函数
void request_render()
{
   s_renderRequested = true;
   s_forceImmediateRender = false;
}

void request_render_immediate()
{
   s_renderRequested = true;
   s_forceImmediateRender = true;
}

bool should_render()
{
   if ( !s_renderRequested )
      return false;
   
   if ( s_forceImmediateRender )
      return true;
   
   u32 now = get_current_timestamp_ms();
   if ( now < s_lastRenderTime + s_minRenderInterval )
      return false;
   
   return true;
}

void execute_render()
{
   render_all(g_TimeNow, false, false);
   s_lastRenderTime = get_current_timestamp_ms();
   s_renderRequested = false;
   s_forceImmediateRender = false;
}
```

**改动2: 修改主循环** (main_loop_r_central函数)
```cpp
// 找到原有的render_all调用，替换为调度器调用
// 原代码:
// render_all(g_TimeNow, false, false);

// 新代码:
if ( should_render() )
{
   execute_render();
}
```

**位置**: `code/r_central/ruby_central.h`

**改动: 导出渲染调度函数**
```cpp
// 在文件末尾添加导出声明
#ifdef __cplusplus
extern "C" {
#endif

// 新增: 渲染调度函数导出
void request_render();
void request_render_immediate();

#ifdef __cplusplus
}
#endif
```

#### 步骤4-8: 替换render_all调用

**events.cpp** (line 194):
```cpp
// 原代码:
render_all(g_TimeNow);

// 新代码:
request_render();
```

**warnings.cpp** (line 330):
```cpp
// 原代码:
render_all(g_TimeNow);

// 新代码:
request_render_immediate();  // 告警需要立即显示
```

**menu_controller_update.cpp** (多处):
```cpp
// 原代码:
render_all(g_TimeNow);

// 新代码:
request_render_immediate();  // 更新进度需要立即显示
```

**menu_search.cpp** (多处):
```cpp
// 原代码:
render_all(get_current_timestamp_ms(), true);

// 新代码:
request_render_immediate();  // 搜索状态需要立即显示
```

**forward_watch.cpp** (多处):
```cpp
// 原代码:
render_all(g_TimeNow);

// 新代码:
request_render();  // 监控状态可以延迟渲染
```

**video_playback.cpp**:
```cpp
// 保持原有调用，不改动
// 视频播放使用独立渲染路径
render_all(get_current_timestamp_ms(), true);
```

#### 步骤10-13: osd.cpp 数值变化检测

**位置**: `code/r_central/osd/osd.cpp`

**改动1: 添加变化检测变量** (文件顶部)
```cpp
// === 新增: 数值变化检测变量 ===
static float s_lastVoltage = -9999.0f;
static float s_lastCurrent = -9999.0f;
static float s_lastSpeed = -9999.0f;
static float s_lastAltitude = -9999.0f;
static int s_lastHeading = -9999;
static int s_lastSatellites = -9999;
static u32 s_lastFlightMode = 0xFFFFFFFF;

// 变化检测阈值
#define VOLTAGE_CHANGE_THRESHOLD 0.1f   // 电压变化阈值 0.1V
#define CURRENT_CHANGE_THRESHOLD 0.1f   // 电流变化阈值 0.1A
#define SPEED_CHANGE_THRESHOLD    1.0f  // 速度变化阈值 1单位
#define ALTITUDE_CHANGE_THRESHOLD 0.5f  // 高度变化阈值 0.5单位
#define HEADING_CHANGE_THRESHOLD  1     // 航向变化阈值 1度
```

**改动2: 修改电压显示函数** (osd_show_voltage)
```cpp
void osd_show_voltage(float x, float y, float voltage, bool bRightAlign)
{
   Model* pActiveModel = osd_get_current_data_source_vehicle_model();
   if ( (NULL == pActiveModel) || (pActiveModel->telemetry_params.fc_telemetry_type == TELEMETRY_TYPE_MSP) )
      return;

   if ( g_pCurrentModel->osd_params.osd_flags2[osd_get_current_layout_index()] & OSD_FLAG2_LAYOUT_LEFT_RIGHT )
      bRightAlign = false;

   // === 新增: 检测告警状态 ===
   bool bWarning = g_VehiclesRuntimeInfo[osd_get_current_data_source_vehicle_index()].bWarningBatteryVoltage;
   
   // === 新增: 数值变化检测 ===
   if ( !bWarning && fabs(voltage - s_lastVoltage) < VOLTAGE_CHANGE_THRESHOLD )
      return;  // 非告警状态且变化太小，不重绘
   
   s_lastVoltage = voltage;

   // 原有绘制逻辑保持不变...
   char szBuff[32];
   // ...
}
```

**改动3: 修改速度显示函数** (在osd_render_elements中)
```cpp
// 在显示速度的位置添加变化检测
float speed = g_VehiclesRuntimeInfo[osd_get_current_data_source_vehicle_index()].headerFCTelemetry.hspeed/100.0f-1000.0;
if ( fabs(speed - s_lastSpeed) >= SPEED_CHANGE_THRESHOLD )
{
   s_lastSpeed = speed;
   // 原有绘制逻辑...
}
```

**改动4: 修改高度显示函数** (在osd_render_elements中)
```cpp
// 在显示高度的位置添加变化检测
float altitude = _osd_convertHeightMeters(g_VehiclesRuntimeInfo[...].headerFCTelemetry.altitude_abs/100.0f-1000.0);
if ( fabs(altitude - s_lastAltitude) >= ALTITUDE_CHANGE_THRESHOLD )
{
   s_lastAltitude = altitude;
   // 原有绘制逻辑...
}
```

---

## 4. 执行顺序

### 4.1 第一阶段执行顺序

```
1. 读取 render_engine_cairo.h
2. 修改 render_engine_cairo.h - 添加成员变量
3. 读取 render_engine_cairo.cpp (构造函数)
4. 修改 render_engine_cairo.cpp - 初始化新变量
5. 读取 render_engine_cairo.cpp (startFrame)
6. 修改 render_engine_cairo.cpp - startFrame改动
7. 读取 render_engine_cairo.cpp (endFrame)
8. 修改 render_engine_cairo.cpp - endFrame改动
9. 编译验证
```

### 4.2 第二阶段执行顺序

```
1. 读取 ruby_central.cpp (顶部)
2. 修改 ruby_central.cpp - 添加调度器
3. 读取 ruby_central.cpp (主循环)
4. 修改 ruby_central.cpp - 主循环改动
5. 读取 ruby_central.h
6. 修改 ruby_central.h - 导出函数
7. 读取 events.cpp
8. 修改 events.cpp - 替换render_all
9. 读取 warnings.cpp
10. 修改 warnings.cpp - 替换render_all
11. 读取 menu_controller_update.cpp
12. 修改 menu_controller_update.cpp - 替换render_all
13. 读取 menu_search.cpp
14. 修改 menu_search.cpp - 替换render_all
15. 读取 forward_watch.cpp
16. 修改 forward_watch.cpp - 替换render_all
17. 读取 osd.cpp (顶部)
18. 修改 osd.cpp - 添加变化检测变量
19. 读取 osd.cpp (电压显示)
20. 修改 osd.cpp - 电压变化检测
21. 编译验证
```

---

## 5. 验证清单

### 5.1 第一阶段验证

| 验证项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 编译通过 | `make all RUBY_BUILD_ENV=radxa` | 无编译错误 |
| Context复用生效 | 添加日志输出 | 日志显示Context复用次数 |
| 双缓冲切换正确 | 触发缓冲区切换 | 无渲染错误 |
| 功能正常 | 运行程序测试 | UI显示正确 |

### 5.2 第二阶段验证

| 验证项 | 验证方法 | 预期结果 |
|--------|----------|----------|
| 编译通过 | `make all RUBY_BUILD_ENV=radxa` | 无编译错误 |
| 编译通过 | `make all RUBY_BUILD_ENV=pi` | 无编译错误 |
| 渲染调度生效 | 添加日志输出 | 帧率稳定在60fps |
| 告警立即显示 | 触发告警 | 告警立即出现 |
| 数值更新正确 | 观察数值变化 | 数值实时更新 |
| 功能正常 | 运行程序测试 | UI显示正确 |

---

## 6. 回滚方案

### 6.1 第一阶段回滚

```cpp
// 在 render_engine_cairo.cpp 中添加开关
#define CAIRO_CONTEXT_REUSE_ENABLED 1

#if CAIRO_CONTEXT_REUSE_ENABLED
   // 优化实现
#else
   // 原始实现
#endif
```

### 6.2 第二阶段回滚

```cpp
// 在 ruby_central.cpp 中添加开关
#define UNIFIED_RENDER_SCHEDULER_ENABLED 1

#if UNIFIED_RENDER_SCHEDULER_ENABLED
   // 优化实现
#else
   // 原始实现
#endif

// 在 osd.cpp 中添加开关
#define VALUE_CHANGE_DETECTION_ENABLED 1

#if VALUE_CHANGE_DETECTION_ENABLED
   // 优化实现
#else
   // 原始实现
#endif
```

---

*文档生成日期: 2026年4月26日*
*RubyFPV 版本: 11.8 (Build 11801)*